// Объявляем функцию
function addNumbers(a, b) { // <-- параметры a и b
  const result = a + b;     // <-- невероятно сложный подсчет результата
  return result;            // <-- возврат результата
}
